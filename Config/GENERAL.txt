#自用插件
#CONFIG_USE_APK=n # 禁用 APK 包管理器
#CONFIG_PACKAGE_luci-lib-ipkg=y # LuCI IPKG 软件包管理库
CONFIG_PACKAGE_zoneinfo-asia=y # 亚洲时区信息支持
CONFIG_PACKAGE_luci-mod-rpc=y # LuCI RPC 远程过程调用支持
CONFIG_PACKAGE_luci-app-sqm=y # SQM 智能队列管理（QoS）
CONFIG_PACKAGE_luci-app-store=y # 软件商店插件
#zram
CONFIG_PACKAGE_zram-swap=y
CONFIG_PACKAGE_kmod-lib-lz4=y
CONFIG_KERNEL_ZRAM_BACKEND_LZ4=y
CONFIG_KERNEL_ZRAM_DEF_COMP_LZ4=y
#科学插件调整
CONFIG_PACKAGE_luci-app-momo=y
CONFIG_PACKAGE_luci-app-nikki=n
CONFIG_PACKAGE_luci-app-openclash=n
CONFIG_PACKAGE_luci-app-passwall2=n
CONFIG_PACKAGE_luci-app-homeproxy=n
#增加插件
CONFIG_PACKAGE_luci-app-upnp=n
CONFIG_PACKAGE_luci-app-ttyd=n
CONFIG_PACKAGE_luci-app-samba4=n
CONFIG_PACKAGE_luci-app-podman=n
CONFIG_PACKAGE_luci-app-mosdns=n
CONFIG_PACKAGE_luci-app-ddns-go=n
CONFIG_PACKAGE_luci-app-cpufreq=n
CONFIG_PACKAGE_luci-app-partexp=n
CONFIG_PACKAGE_luci-app-gecoosac=n
CONFIG_PACKAGE_luci-app-wolultra=n
CONFIG_PACKAGE_luci-app-easytier=n
CONFIG_PACKAGE_luci-app-tailscale=n
CONFIG_PACKAGE_luci-app-quickfile=n
CONFIG_PACKAGE_luci-app-netwizard=n
CONFIG_PACKAGE_luci-app-autoreboot=n
CONFIG_PACKAGE_luci-app-timecontrol=n
CONFIG_PACKAGE_luci-app-mini-diskmanager=n
#删除插件
CONFIG_PACKAGE_luci-app-attendedsysupgrade=n # 自动升级已关闭
CONFIG_PACKAGE_luci-app-wol=n # WOL 已关闭
#参数调整
CONFIG_CCACHE=y # 启用编译缓存
CONFIG_DEVEL=y # 包含开发工具
CONFIG_FEED_video=n # 禁用视频源
CONFIG_TARGET_MULTI_PROFILE=y # 多配置文件支持
CONFIG_TARGET_PER_DEVICE_ROOTFS=y # 每个设备独立文件系统
CONFIG_TARGET_ROOTFS_INITRAMFS=n # 不生成 Initramfs
#内核调整
CONFIG_PACKAGE_kmod-ata-ahci=n # 禁用 SATA AHCI 驱动
CONFIG_PACKAGE_kmod-ata-core=n # 禁用 ATA 核心驱动
CONFIG_PACKAGE_kmod-bonding=y # 网络链路聚合
CONFIG_PACKAGE_kmod-dsa=y # DSA 交换机驱动
CONFIG_PACKAGE_kmod-dummy=y # 虚拟网络驱动
CONFIG_PACKAGE_kmod-fs-btrfs=y # Btrfs 文件系统（Docker 推荐）
CONFIG_PACKAGE_kmod-fs-ksmbd=y # KSMBD 内核态共享
CONFIG_PACKAGE_kmod-fuse=y # FUSE 用户态文件系统
CONFIG_PACKAGE_kmod-inet-diag=y # 网络诊断
CONFIG_PACKAGE_kmod-inet-mptcp-diag=y # 多路径 TCP 诊断
CONFIG_PACKAGE_kmod-mtd-rw=y # MTD 读写支持
CONFIG_PACKAGE_kmod-netlink-diag=y # Netlink 诊断
CONFIG_PACKAGE_kmod-nf-nat6=y # IPv6 NAT 支持
CONFIG_PACKAGE_kmod-nft-arp=y # NFT ARP 过滤
CONFIG_PACKAGE_kmod-nft-bridge=y # NFT 网桥过滤
CONFIG_PACKAGE_kmod-nft-connlimit=y # NFT 连接限制
CONFIG_PACKAGE_kmod-nft-core=y # NFT 核心
CONFIG_PACKAGE_kmod-nft-dup-inet=y # NFT 镜像流量
CONFIG_PACKAGE_kmod-nft-fib=y # NFT 路由查找
CONFIG_PACKAGE_kmod-nft-fullcone=y # NFT 全锥 NAT
CONFIG_PACKAGE_kmod-nft-nat=y # NFT NAT 转换
CONFIG_PACKAGE_kmod-nft-netdev=y # NFT 网卡设备过滤
CONFIG_PACKAGE_kmod-nft-offload=y # NFT 硬件卸载
CONFIG_PACKAGE_kmod-nft-queue=y # NFT 队列处理
CONFIG_PACKAGE_kmod-nft-socket=y # NFT 套接字匹配
CONFIG_PACKAGE_kmod-nft-tproxy=y # NFT 透明代理
CONFIG_PACKAGE_kmod-nft-xfrm=y # NFT IPsec 流量过滤
CONFIG_PACKAGE_kmod-nvme=n # 禁用 NVMe 驱动
CONFIG_PACKAGE_kmod-ovpn-backports=y # OpenVPN 后端支持
CONFIG_PACKAGE_kmod-sound-core=n # 禁用声卡核心驱动
CONFIG_PACKAGE_kmod-tun=y # TUN 虚拟网卡（Docker/VPN 必需）
CONFIG_PACKAGE_kmod-usb3=n # 禁用 USB 3.0
CONFIG_PACKAGE_kmod-usb-audio=n # 禁用 USB 音频
CONFIG_PACKAGE_kmod-usb-core=n # 禁用 USB 核心
CONFIG_PACKAGE_kmod-usb-dwc3=n # 禁用 USB 控制器
CONFIG_PACKAGE_kmod-usb-net=n # 禁用 USB 网卡
CONFIG_PACKAGE_kmod-usb-net-asix=n # 禁用 USB ASIX 网卡
CONFIG_PACKAGE_kmod-usb-net-asix-ax88179=n # 禁用 USB 网卡驱动
CONFIG_PACKAGE_kmod-usb-net-cdc-eem=n # 禁用 USB EEM 驱动
CONFIG_PACKAGE_kmod-usb-net-cdc-ether=n # 禁用 USB Ether 驱动
CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=n # 禁用 USB MBIM 驱动
CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=n # 禁用 USB NCM 驱动
CONFIG_PACKAGE_kmod-usb-net-cdc-subset=n # 禁用 USB Subset 驱动
CONFIG_PACKAGE_kmod-usb-net-huawei-cdc-ncm=n # 禁用华为 USB 网卡驱动
CONFIG_PACKAGE_kmod-usb-net-ipheth=n # 禁用 iOS 网络共享
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=n # 禁用 QMI WWAN 驱动
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-fibocom=n # 禁用广和通驱动
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-quectel=n # 禁用移远驱动
CONFIG_PACKAGE_kmod-usb-net-rndis=n # 禁用 RNDIS 驱动
CONFIG_PACKAGE_kmod-usb-net-rtl8150=n # 禁用 RTL8150 驱动
CONFIG_PACKAGE_kmod-usb-net-rtl8152=n # 禁用 RTL8152 驱动
CONFIG_PACKAGE_kmod-usb-net-sierrawireless=n # 禁用 Sierra 驱动
CONFIG_PACKAGE_kmod-usb-ohci=n # 禁用 OHCI 驱动
CONFIG_PACKAGE_kmod-usb-storage=n # 禁用 USB 存储
CONFIG_PACKAGE_kmod-usb-storage-extras=n # 禁用额外存储支持
CONFIG_PACKAGE_kmod-usb-storage-uas=n # 禁用 UAS 协议
CONFIG_PACKAGE_kmod-usb-uhci=n # 禁用 UHCI 驱动
CONFIG_PACKAGE_kmod-usb-xhci=n # 禁用 XHCI 驱动
CONFIG_PACKAGE_kmod-veth=y # 虚拟以太网对（Docker 容器联网必需）
CONFIG_PACKAGE_kmod-wireguard=y # WireGuard VPN 驱动
#组件调整
CONFIG_PACKAGE_autocore=y # 系统负载统计
CONFIG_PACKAGE_automount=y # 自动挂载工具
CONFIG_PACKAGE_blkid=y # 块设备 UUID 识别
CONFIG_PACKAGE_cfdisk=n # 禁用冗余分区工具
CONFIG_PACKAGE_cgdisk=n # 禁用冗余 GPT 工具
CONFIG_PACKAGE_coremark=n # 禁用 CPU 跑分
CONFIG_PACKAGE_cpufreq=y # 频率调节
CONFIG_PACKAGE_curl=y # 命令行传输
CONFIG_PACKAGE_dmesg=y # 内核信息记录
CONFIG_PACKAGE_exfat-fsck=n # 禁用 exFAT 修复
CONFIG_PACKAGE_exfat-mkfs=n # 禁用 exFAT 格式化
CONFIG_PACKAGE_fdisk=y # 分区工具（管理 eMMC/NAND 必需）
CONFIG_PACKAGE_gdisk=y # GPT 分区工具（管理 eMMC/NAND 必需）
CONFIG_PACKAGE_htop=y # 系统进程监控
CONFIG_PACKAGE_iperf3=y # 网络测速
CONFIG_PACKAGE_ip-full=y # 完整版 IP 指令集
CONFIG_PACKAGE_libimobiledevice=n # 禁用苹果设备驱动
CONFIG_PACKAGE_libnvme=n # 禁用 NVMe 库
CONFIG_PACKAGE_lsblk=y # 查看块设备列表
CONFIG_PACKAGE_luci-base=y # LuCI 基础环境
CONFIG_PACKAGE_luci-compat=y # LuCI 兼容性
CONFIG_PACKAGE_luci-lib-base=y # LuCI 核心库
CONFIG_PACKAGE_luci-lib-ipkg=y # LuCI 包管理库
CONFIG_PACKAGE_luci-lua-runtime=y # Lua 环境
CONFIG_PACKAGE_luci-proto-relay=y # 协议中继
CONFIG_PACKAGE_mmc-utils=y # eMMC 工具
CONFIG_PACKAGE_nand-utils=y # NAND 存储工具
CONFIG_PACKAGE_nvme-cli=n # 禁用 NVMe 管理指令
CONFIG_PACKAGE_openssh-keygen=y # SSH 密钥生成
CONFIG_PACKAGE_openssh-sftp-server=y # SFTP 服务
CONFIG_PACKAGE_openssl-util=y # OpenSSL 指令
CONFIG_PACKAGE_proto-bonding=y # 链路聚合协议
CONFIG_PACKAGE_sfdisk=n # 禁用冗余分区指令
CONFIG_PACKAGE_sgdisk=n # 禁用冗余 GPT 指令
CONFIG_PACKAGE_smartmontools=n # 禁用闪存 SMART 监控
CONFIG_PACKAGE_smartmontools-drivedb=n # 禁用 SMART 数据库
CONFIG_PACKAGE_usb-modeswitch=n # 禁用 USB 模式切换
CONFIG_PACKAGE_usbmuxd=n # 禁用 USB 复用
CONFIG_PACKAGE_usbutils=n # 禁用 USB 工具
CONFIG_PACKAGE_xz-utils=y # XZ 解压缩工具
